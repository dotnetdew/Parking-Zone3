@using Parking_Zone.Enums
@model Parking_Zone.ViewModels.ParkingSlot.ParkingSlotCreateVM

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ParkingSlot</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" value="@Model.ParkingZoneId" asp-for="ParkingZoneId"/>
            <input type="hidden" value="@Model.ParkingZoneName" asp-for="ParkingZoneName"/>
            <div class="form-group">
                <label asp-for="Number" class="control-label"></label>
                <input asp-for="Number" class="form-control" />
                <span asp-validation-for="Number" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsAvailableForBooking" /> @Html.DisplayNameFor(model => model.IsAvailableForBooking)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="Category" class="control-label"></label>
                <select asp-for="Category" class="form-select" asp-items="Html.GetEnumSelectList<SlotCategoryEnum>()"></select>
                <span asp-validation-for="Category" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-parkingZoneId="@Model.ParkingZoneId">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
